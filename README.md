# chatbot-test

## Server

```bash
cd server
source venv/bin/activate        # Activate the virtual environment
uvicorn app.main:app --reload   # Start the dev server on port 8000
```

## Client

```bash
  cd client
  npm run dev
```
